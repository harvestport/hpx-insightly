from setuptools import setup

setup(name='insightly',
	  description='thin python wrapper around insightly api',
	  url='https://github.com/harvestport/insightly-python',
	  packages=['insightly']
	  )